#pragma once

#include <functional>
#include <utility>

namespace Glue
{
    //template <typename TReturn, typename ...TArgs>
    //public ref class DelegateBinder
    //{
    //public:
    //    DelegateBinder();

    //    TReturn Invoke(TArgs&& ...args)
    //    {
    //        return m_target(std::forward<TArgs>(args)...);
    //    }

    //private:
    //    std::function<TReturn(TArgs...)>& m_target;
    //};
}